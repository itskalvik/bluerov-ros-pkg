/**:
  ros__parameters:
    plugin_denylist:
      # common
      - '*'

    plugin_allowlist:
      - 'imu'
      - 'sys_*'
      - 'rc_io'
      - 'command'
      - 'local_position'
      - 'setpoint_position'
      - 'setpoint_velocity'
      